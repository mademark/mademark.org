# macOS
.DS_Store

# Version archives — tracked via git tags instead; zips kept locally in _releases/
_releases/

# Local-only Claude Code settings
.claude/settings.local.json

# Deployment credentials (real file gitignored; deploy.env.example is tracked)
deploy.env
